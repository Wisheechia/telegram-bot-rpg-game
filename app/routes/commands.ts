import start from "../functions/commands/start";
import launch from "../functions/commands/launch";


const commands = {
	start,
    launch
};

export {
	start,
    launch
};

export default commands;